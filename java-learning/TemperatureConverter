import java.util.Scanner;

public class TemperatureConverter{

public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);

        // --- Get the first number ---
        // We use 'double' instead of 'int' so decimals like 3.14 work.
        System.out.print("Enter a temperature: ");
        double temp1 = scanner.nextDouble();
        // Ask whether it is Celsius or Fahrenheit
        System.out.print("Please type in a C if the temperature is in Celsius. Please type in an F if the temperature is in Fahrenheit: ");
        char CorF = scanner.nextChar();

        double result;

        if (char == 'C') {
            result = temp1;
        } else if (operator == '-') {
            result = num1 - num2;

    

